import { type CSSProperties } from 'react'

export const layoutContainer: CSSProperties = {
  display: 'flex',
}

export const desktopContent: CSSProperties = {
  marginRight: '20%',
  width: '80%',
}

export const mobileContent: CSSProperties = {
  width: '100%',
  paddingBottom: '10%',
}
